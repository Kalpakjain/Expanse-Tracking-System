# Frontend

Next.js starter for the Smart Expense Tracker dashboard.

## Run locally

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` before running if the API is not on `http://localhost:8000`.

