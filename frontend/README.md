# Frontend

Next.js app for the Smart Expense Tracker dashboard, reports, receipt review, account management,
savings suggestions, and notification previews.

## Run locally

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` before running if the API is not on `http://localhost:8000`.
